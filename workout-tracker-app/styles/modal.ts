import { StyleSheet } from "react-native";

export const modalStyles = StyleSheet.create({
    view: {
        flex: 1,
        padding: 20,
        paddingTop: 50,
        backgroundColor: '#3B3936',
    }
});